<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= render 'layouts/head' %>
  </head>

  <body class="<%= hide_sidebar? ? 'state-sidebar-hidden' : 'state-sidebar-visible' %>">
    <%= render 'layouts/alert' %>
    <div class="wrapper">
      <%= render 'layouts/header' %>
      <%= yield %>
      <div id="remote_modal_wrapper"></div>
    </div>  
  </body>
</html>
