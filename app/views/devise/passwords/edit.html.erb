<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6 col-xl-5">

      <h2 class="text-center mb-4 fw-bold">新しいパスワードの設定</h2>

      <div class="card shadow-sm">
        <div class="card-body p-4">
          
          <p class="text-muted small mb-4 text-center">
            新しいパスワードを入力してください。<br>
            設定完了後、そのままログインいただけます。
          </p>

          <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>
            
            <%= f.hidden_field :reset_password_token %>

            <div class="mb-3">
              <div class="d-flex justify-content-between align-items-center mb-1">
                <%= f.label :password, "新しいパスワード", class: "form-label fw-bold mb-0" %>
                <% if @minimum_password_length %>
                  <span class="badge bg-light text-dark border">
                    <%= @minimum_password_length %>文字以上
                  </span>
                <% end %>
              </div>
              <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control", placeholder: "新しいパスワードを入力" %>
            </div>

            <div class="mb-4">
              <%= f.label :password_confirmation, "新しいパスワード（確認用）", class: "form-control-label fw-bold mb-1" %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "もう一度入力してください" %>
            </div>

            <div class="d-grid">
              <%= f.submit "パスワードを更新してログイン", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

        </div>
      </div>

      <div class="text-center mt-3 small">
        <%= render "devise/shared/links" %>
      </div>

    </div>
  </div>
</div>
