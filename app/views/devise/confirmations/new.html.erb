<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6 col-xl-5">

      <h2 class="text-center mb-4 fw-bold">承認メールの再送</h2>

      <div class="card shadow-sm">
        <div class="card-body p-4">
          
          <p class="text-muted small mb-4 text-center">
            アカウント登録を完了するための<br>承認メールを再送いたします。
          </p>

          <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>

            <div class="mb-4">
              <%= f.label :email, "メールアドレス", class: "form-control-label fw-bold mb-1" %>
              <%= f.email_field :email, 
                  autofocus: true, 
                  autocomplete: "email", 
                  class: "form-control", 
                  placeholder: "example@email.com",
                  value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email) %>
              <div class="form-text text-muted">ご登録済みのメールアドレスを入力してください。</div>
            </div>

            <div class="d-grid">
              <%= f.submit "承認メールを再送する", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

        </div>
      </div>

      <div class="text-center mt-3 small">
        <%= render "devise/shared/links" %>
      </div>

    </div>
  </div>
</div>
