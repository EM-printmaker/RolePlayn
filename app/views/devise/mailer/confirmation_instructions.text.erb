<%= t(".greeting", recipient: @email) %>

<%= t(".instruction") %>

■<%= t(".action") %> URL
<%= confirmation_url(@resource, confirmation_token: @token) %>

──────────────────────────────────────
※このメールに心当たりがない場合は、お手数ですがこのまま破棄してください。
※リンクの有効期限が切れた場合は、ログイン画面から再送をリクエストしてください。
──────────────────────────────────────
